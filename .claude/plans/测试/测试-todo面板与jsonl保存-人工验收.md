# 测试记录：todo 面板 + jsonl 追加式保存 人工验收

> **日期**：2026-09-02
> **前置**：代码实施完成（D1-D3），构建 161/161、ctest 20/20、主程序冒烟通过
> **编号**：UI n = 《设计-任务清单UI显示.md》§五 第 n 条；J n = 《设计-会话追加式保存.jsonl.md》§五 第 n 条
> **程序**：`bin/Debug/CLFCode.exe`；会话文件目录：`doc/contextHistory/`

## 验收发现与修复记录

- **BUG-1（已修复，2026-09-02）**：场景 A 第三步（update 第一项 in_progress）时任务面板消失。
  - 根因：submit 的"新回合清空"置 `m_todoPanelDone=true` 后，todo_write **update** 分支只做了快照落盘、未清面板隐藏标志（仅 create 清了）——跨轮场景面板随 update 无法重现（dsh projection 语义：任何 todo/write 事件重建投影）
  - 修复：update 分支补 `setTodoPanelDone(false)`（与 create 一致）；设计文档 §3.3 update 行同步；qa_CLFBuiltinTools 新增 B4 回归用例（todoWriteHandlerImpl 暴露为单测入口）；ctest 20/20
  - 请从场景 A 重新执行 A1-A8

---

## 场景 A：面板完整生命周期（约 10 分钟）

> 覆盖：UI 1-8、10、15、24；J 22

| ☐ | 步骤 | 预期 | 编号 |
|---|---|---|---|
| ☐ | A1 启动程序（无旧清单） | 界面**无任务面板、无占位** | UI 1 |
| ☐ | A2 让 agent 用 todo_write 建 3 项任务 | 面板立即出现：`📋 任务清单 0/3`，3 行全 `○` 灰 | UI 2 |
| ☐ | A3 让 agent 把第 1 项标 in_progress | 该行变 `⏳` 高亮（CyanLight），计数不变；**原地更新无追加** | UI 3 |
| ☐ | A4 让 agent 把第 1 项标 completed | 该行变 `✓` 绿，计数 `1/3` | UI 4 |
| ☐ | A5 全程观察对话流 | **无任何清单块追加进对话流**（工作流区只有对话/工具输出） | UI 5 |
| ☐ | A6 让 agent 完成剩余 2 项（全 ✓） | 本轮结束自动：对话流追加一行 `📋 任务清单（全部完成）: ✓ A · ✓ B · ✓ C` → **面板清空消失** | UI 6 |
| ☐ | A7 完成后继续输入新对话（不建新任务） | 面板保持消失；收尾行在对话流历史可回看 | UI 7、24 |
| ☐ | A8 未全完成时让 agent 调 todo_write clear | 面板立即消失 | UI 8 |
| ☐ | A10 工具执行中观察（读类工具 showProgress 动画时） | 任务面板与进度行**同屏正常**（面板在进度行上方） | UI 10 |
| ☐ | A15 全完成收尾时观察顺序 | 模型总结 → `📋 任务清单（全部完成）` → `✻ Worked for Ns`（清单行紧跟总结、在 worked **前**） | UI 15 |
| ☐ | A22 全完成轮后打开会话 jsonl 文件 | `complete` 行出现在该轮 `turn` 行**之前**（先写 complete 再轮末 turn） | J 22 |

---

## 场景 B：回合边界与中断（约 10 分钟）

> 覆盖：UI 12、14、16-18、22、25；J 23

| ☐ | 步骤 | 预期 | 编号 |
|---|---|---|---|
| ☐ | B18 清单未完成（如 1✓ 2⏳ 3○）回合结束后，输入新对话 | **面板清空**（UI 不显示）；jsonl 历史仍有该轮快照 | UI 18 |
| ☐ | B12 清单未全完成时 Esc 中断 | 面板**保留未完成状态**；不追加不清空；**无 ✻ worked** | UI 12 |
| ☐ | B14 模型主动停但未标完（finish_reason=stop 且清单非全✓） | **有 ✻ worked**，面板保留 | UI 14 |
| ☐ | B16 清单 1✓ 2⏳ 3○ 4○ 5○ 时对 agent 说"继续做 2 3" | 模型若 update → 原清单保留只改 2；若 create 只写 2 3 → 整表替换（1 4 5 消失）——**UI 只呈现不干预** | UI 16 |
| ☐ | B17 查看 todo_write 的工具说明 | 文案含"继续已有任务时用 update，不要用 create 重建；create 为整表替换，仅用于全新清单" | UI 17 |
| ☐ | B22 有未完成清单时 `/clear` | 面板消失 + 新会话无残留（新会话中模型 list 为空） | UI 22 |
| ☐ | B23 一轮内只让模型调 todo_write **list**（不改数据） | 该轮 jsonl turn 行**无 todos 字段**（list 不置 m_todoDirty） | J 23 |
| ☐ | B25 清单最后一个 update 后立刻 Esc（全✓中断）→ 输入新对话 | 面板残留被**新回合清空**；对话流无收尾行（符合"被打断"语义） | UI 25 |

---

## 场景 C：jsonl 文件结构（约 5 分钟）

> 覆盖：J 1-6、11、17、21、25、26

| ☐ | 步骤 | 预期 | 编号 |
|---|---|---|---|
| ☐ | C1 启动后**不输入任何对话**直接退出 | `doc/contextHistory/` 下**不产生新文件** | J 11 |
| ☐ | C2 输入第一条对话 | 生成 `时间戳_标题.jsonl`，首行为 `{"type":"header",...}`（含 title/session_id/model） | J 1 |
| ☐ | C3 对话一轮（无清单操作） | 结束追加一行 `{"type":"turn",...}`，**无 todos 字段** | J 2、4 |
| ☐ | C4 建清单并逐步 update 一轮 | **每次 update 即时追加 `{"type":"todo_snapshot",...}` 行**（不等轮末）；轮末 turn 行带 todos 快照 | J 3 |
| ☐ | C5 中途打开会话文件查看 | turn 行与 todo_snapshot 行交错，时间戳递增、**行序 = 发生顺序** | J 5 |
| ☐ | C6 `/exit` 退出 | **无改名、无归档、无搬移**——文件名字从头到尾不变 | J 6、17 |
| ☐ | C21 全完成收尾后看文件 | 文件追加 `{"type":"complete","todos":[...]}` 行 | J 21 |
| ☐ | C25 会话中 `/clear` 后看文件 | 文件尾追加 `{"type":"summary",...}` 行（摘要开关开时） | J 25 |
| ☐ | C26 `/exit` 关闭的会话文件 | 无 summary 行（摘要只在 /clear 生成） | J 26 |

---

## 场景 D：resume 查看 / 续写 / 冻结（约 10 分钟）

> 覆盖：UI 9、19-21；J 7、8、12-16、29

| ☐ | 步骤 | 预期 | 编号 |
|---|---|---|---|
| ☐ | D7 重启 + `/resume` 一个多轮会话（对话 2 建过清单） | 折叠块内：完整消息 + **对话 2 末尾有 `📋 本轮清单 n/total：...` 行**（对话 1/3 无） | J 7 |
| ☐ | D8 resume 后清单状态 | = **最后一条 todo_snapshot**（若中途 update 未到轮末，取它而非 turn 行快照） | J 8 |
| ☐ | D9 未完成会话 resume | 面板**原样重现**（含各状态 ✓/⏳/○） | UI 9、19 |
| ☐ | D20 全完成会话 resume | 面板**不显示**（置位；完成记录在折叠块历史里） | UI 20 |
| ☐ | D12 `/resume` 会话 1 后**不输入**直接 `/clear` | **零副作用**——`contextHistory/` 下无新文件 | J 12 |
| ☐ | D13 `/resume` 会话 1 → 输入第一条新对话 | 生成 `时间戳_会话1续.jsonl`（header + 会话 1 全部行副本 + 本轮） | J 13 |
| ☐ | D16 续写后检查会话 1 原始文件 | **内容原样不变**（冻结快照） | J 16 |
| ☐ | D21 resume 未完成会话 → 输入"继续做 2 3" | 第一条续写输入**面板保留**（不清空）→ 模型 list 确认 → 完成 2 3 → 全完成收尾 | UI 21 |
| ☐ | D29 resume 续写会话输入第二轮 | 第二轮按**普通新回合**清面板（resumedFrom 已清除） | J 29 |
| ☐ | D14 续写若干轮 → `/exit` → 再 resume 延续会话 | 看到从会话 1 开头到续写结尾的**完整对话** | J 14 |
| ☐ | D15 会话 1 末轮 todos = 1/3 → 续写完成后 resume | 清单从 1/3 演进到最终态（每轮快照可见） | J 15 |

---

## 场景 E：列表与 [当前] 标记（约 3 分钟）

> 覆盖：J 27、28；J 9（旧 latest.json 兼容，可选）

| ☐ | 步骤 | 预期 | 编号 |
|---|---|---|---|
| ☐ | E28 进行中会话（已建 .jsonl）时 `/history` 与 `/resume` | 当前会话在列表中标记 **[当前]**（与归档混排、不排除） | J 28 |
| ☐ | E27 `/resume` 选 [当前] 会话 → 输入新对话 | 生成"续"文件，后续 turn 只写续文件；**原文件冻结不再被写** | J 27 |
| ☐ | E9 若目录有旧版 `latest.json`（覆盖式时代） | 照常可 resume（兼容路径），列表显示正常 | J 9 |

---

## 场景 F：崩溃与损坏（约 5 分钟）

> 覆盖：J 18-20（关键）；J 10、23、24（可选）

| ☐ | 步骤 | 预期 | 编号 |
|---|---|---|---|
| ☐ | F20 **（关键）**同轮建清单 1-5 + 执行到第 4 步时**杀进程**（任务管理器或 Ctrl+C 强杀） | 文件含 create/update 的 todo_snapshot 行 → 重启 `/resume` → **m_todos = [1✓ 2✓ 3✓ 4✓ 5○]**（进度保留，非全丢） | J 20 |
| ☐ | F18 turn6 进行中杀进程 | 文件含 turn1-5 → 重启 `/resume` → 显示 turn1-5，turn6 不存在（非"丢失"是"从未写入"） | J 18 |
| ☐ | F19 崩溃残留文件存在时启动 → 新对话 | 新对话另开新文件；残留文件原样保留 | J 19 |
| ☐ | F10 故意破坏 jsonl 中间一行（记事本乱改）→ resume | 跳过该行不崩溃，其余正常 | J 10 |
| ☐ | F23 故意制造 todo_snapshot 撕裂行（删半行）→ resume | 取前一条可解析快照（进度回退一格，不崩溃） | J 23 |
| ☐ | F24 磁盘满 | 写入失败 → warn 日志 + 不崩溃（**可选**，难模拟可跳过） | J 24 |

---

## 验收结果汇总

| 场景 | 应过 | 实过 | 失败条目与现象 |
|---|---|---|---|
| A 面板生命周期 | 9 | | |
| B 回合边界与中断 | 8 | | |
| C jsonl 文件结构 | 8 | | |
| D resume 续写冻结 | 11 | | |
| E 列表与 [当前] | 3 | | |
| F 崩溃与损坏 | 4 必测 + 2 可选 | | |
| **合计** | **43 必测 + 2 可选** | | |

> 验收发现的 bug 记录在下方（现象 + 复现步骤），验收完成后更新《测试记录》并归档两份设计文档。
