# 设计-阶段1-实施边界清单与取证点

> **生命周期状态**：✅ 取证销号完成（2026-09-03 pro 评审，69 条全部核实——8 条证伪修订、61 条证实/定案）；执行期新发现继续回流本清单
> **创建**：2026-09-03（阶段 1 详细分析：方案层修订已并入分册正文——F1 覆盖矩阵/F2 C1 方向修订/F5 A4 升格，无 F3/F4；本文 = 源码级边界与取证点）
> **配套**：《设计-阶段1-代码审查与模块重构.md》
> **用法**：按批次逐条核对 → 销号表（§六）勾记。每条含：边界问题 / 取证动作（到源码核实什么）/ 建议倾向
> **覆盖范围注明（2026-09-03 全量审查后）**：本清单销号表覆盖 A1-A4/B1-B5/C1-C4/T1-T8；**B6/C5/C6 及全量审查新增 P 项（P0-7/8、P1-13~16、P2-7/8）的取证内联于分册对应批次正文**（全量审查底稿已归档），本清单不重复收录——执行期勿误以为清单 = 全量取证
> **证据分级**：A = 源码可核实；B = 需实测/冒烟验证；C = 设计裁决，pro 评审后定
> **原则**：宁可多列取证，pro 证伪后划掉，不留暗坑

---

## 一、总则

1. 每批独立可交付可回退：建议每批独立分支/提交点，v0.6.0 为回退基线
2. 全量 `ctest` 每批必跑 + T5 交互保真冒烟路径（对话/tool 调用/确认/粘贴/折叠/Ctrl+T/Ctrl+R/恢复会话）每批必跑
3. 改造原则：A 批"纯搬移"仅对 A1/A3 部分成立；A2 是"搬移+语义修正"、A3 牵测试，勿当零风险推
4. 新公共文件（CLFTextUtil 等）的 CMake 归属必须一次定对——历史教训：FileOps/Diff 被"物理编入 clf_core"正是归属没定对的产物（见 C1）
5. 本清单"取证"列命中源码事实后，与清单冲突处**以源码为准并回注本清单**

---

## 二、批次 A 边界

### A1 CLFRepl 拆分（P0-1）

**A1-1 捕获量切分归属**｜A
- 边界：run() 内 Renderer(178–484) + CatchEvent(516–855) 两个闭包共享 ~20 捕获局部量（隐式类字段 + 局部），拆 View/InputHandler 时每个量的归属决定类构造参数
- 取证：逐行列出两闭包实际捕获的变量清单 → 划归三桶：CLFReplView 状态 / CLFInputHandler 状态 / 两者共享（terminal 引用、提交回调）
- 建议：先列清单再动手；共享量收敛为显式状态对象，禁止两边各持副本

**A1-2 事件消费语义保真**｜A
- 边界：CatchEvent 每分支返回 true/false（是否消费/冒泡给 FTXUI）不能变——拆分后事件分发顺序、消费语义必须逐一对照原代码
- 取证：整理按键分发表（提交/粘贴/确认/选区/快捷键/历史/Ctrl+C/Esc/Tab），记录每分支原返回值
- 建议：CLFInputHandler 方法返回 bool 与原来一致；纯搬移，不改任何分支逻辑

**A1-3 折叠状态单点持有**｜A
- 边界：thinking 折叠（Ctrl+T，Repl:706）与 resume-echo 折叠（Ctrl+R，Repl:712→toggleFoldedBlock）跨"渲染读 + 事件写"两侧；拆分后折叠集合不能裂成两份
- 取证：确认两处折叠状态现在的存储位置与读写点
- 建议：折叠状态放 View 或共享状态对象，InputHandler 经方法切换，View 只读渲染

**A1-4 选区/滚动一致性**｜A
- 边界：选区（SelectionModel，含 CJK 宽度 displayWidth）与 Renderer 滚动/光标映射耦合；选区状态被 CatchEvent 修改、被 Renderer 渲染——同一实例两端共享
- 取证：SelectionModel 实例生命周期（run 内局部？类字段？），哪些闭包引用
- 建议：随共享状态对象传递，勿复制

**A1-5 壳层保留项**｜A
- 边界：启动临时文件清理(108–114)、FTXUI 初始化(116–125)、Win32 控制台修复/收尾(858–876) 属 run() 生命周期，不进 View/Handler
- 建议：CLFRepl.cpp 瘦壳 = run 编排 + 组件装配 + 生命周期收尾

**A1-6 printBanner 别误搬**｜A
- 边界：printBanner(893–909) 含 CLFSkillLoader 副作用（P2-1）——是启动动作不是渲染；A1 只搬移不修正，P2 项留维护期
- 取证：printBanner 调用点与依赖
- 建议：留壳层；顺手"修正"P2-1 会破坏批次纯净，禁止

**A1-7 submit 归属待定**｜A
- 边界：submit(911–988) 含会话生命周期策略（P0-4，B2 会大改）——A1 若把它物理搬进 InputHandler，B2 又要从 InputHandler 迁 core；若留壳层则 InputHandler 与壳层职责交叉
- 取证：submit 依赖的捕获量与字段
- 建议：倾向留 CLFRepl 壳层（B2 直接收编），A1 只拆渲染与事件分发主体；pro 裁决

**A1-8 stripCprResidual 两处语义核对**｜A
- 边界：CPR/ANSI 残留剥离两处（183–203 / 825–845）上下文不同（渲染侧 vs 事件/粘贴侧）——若处理对象不同则抽函数需参数化，不能想当然合并
- 取证：对比两段剥离逻辑处理的具体序列与触发场景
- 建议：确认同构才抽 `stripCprResidual(std::string&)`；异构则抽带参版本，A2 统一

### A2 公共字符工具（P0-3/P1-8~11/P1-13 + P0-8 前半 sanitizeUtf8 归位 + handleHttpError）——搬移+语义修正

**A2-1 UTF-8 截断逐点取证（2026-09-03 完成，方案修订）**｜A ✅
- 取证结果（逐调用点判意图）：**当前代码库不存在必须精确字节数的截断点**——
  - ToolExecutor extractKeyParam/formatToolResult 6 处 substr＝显示用途 → 安全版
  - Summarizer:263/272 摘要截断＝文本场景（劈半产生非法 UTF-8 注入 system 提示）→ 安全版
  - WebFetch.cpp:34/38 head/tail 已带边界回退＝已是安全版
  - Repl:270 pendingLine 按字节 wrap＝渲染场景 → 改 `substrByWidth`（与主内容硬换行统一，非截断函数范畴）
  - 安全版现有 7 处（Repl:283、Terminal:23、SessionManager:47/69/147、WebFetch:34/38）
- 修订建议：**统一 `utf8SafeHead` 安全语义（唯一函数）**，替换全部显示/文本截断点；不做字节精确版——未来真出现协议字节预算需求时再加（原"双函数"方案作废）

**A2-2 CJK 两套宽度必须收敛一致**｜A
- 边界：displayWidth（SelectionModel:19–24，选区定位用）与 cjkWidth（Terminal:198–207，渲染用）若实现不等价 → 光标/选区错位。收敛是**正确性要求**不是洁癖
- 取证：对比两套宽度表的字符范围差异（宽字符/emoji/组合字符/控制字符处理）
- 建议：收敛为单一 `charWidth(c)/displayWidth(s)` 两处共用；差异处先立宽度测试用例再合并

**A2-3 splitLinesKeepEmpty 三处空行语义核对**｜A
- 边界：AgentLoop:30–42 vs Terminal:89–101/378–394——换行拆分对 \r\n/\n/\r、末尾空行、连续空行的处理三处未必一致（解析 vs 渲染意图不同）
- 取证：三处调用点各自期望（是否保留末尾空行、\r 处理）
- 建议：签名带参（keepEmpty/规范 CRLF）或统一后逐点核对输出等价

**A2-4 localNow 线程安全与格式等价（2026-09-03 取证完成，边界修正）**｜A ✅
- 取证结果：6 处 = CLFTypes.hpp:226/228+250/252（localDateStamp/localTimeStamp 已有封装）、Passerby:30/32、Logger:31/33、SessionManager:28/30、BuiltinTools:121/125——**全部已用 localtime_s/_r 线程安全函数，无裸 localtime**。原"localtime 非线程安全"断言证伪；收敛动机 = 消除 ifdef 重复（纯重复消除），非线程安全增强
- 修订建议：`localNow(fmt)` 内部维持 localtime_s/_r 实现；各调用点传原 fmt，输出 diff 验证零变化（格式串清单：文件名 %Y-%m-%d_%H-%M-%S / 日志 / %Y-%m-%d %H:%M:%S / HH:mm / YYYY-MM-DD / MM-DD HH:mm）

**A2-5 util 文件归属（防重蹈 CMake hack）**｜A
- 边界：CLFTextUtil 被 core/tools/UI 多处引用——放 CLFTypes（header-only，各 target 可见）还是新 cpp（CMake 归属若只进一个 target = 复刻 FileOps 被编入 clf_core 的老路）
- 取证：CLFTypes 现有 header-only/inline 先例；5 个 target 的引用面
- 建议：纯 header（inline/模板）入 CLFTypes 或薄头；若必须 cpp → 独立目标一次画对，见 C1 同款原则

**A2-6 非法 UTF-8 输入**｜A
- 边界：安全截断遇非法序列/截断点落在 4 字节字符中间/带 BOM——现有 isValidUtf8 在哪、截断前是否校验
- 取证：isValidUtf8 定义位置与现有调用链
- 建议：utf8SafeHead 对非法输入的行为先定（不崩、原样返回 or 替换符），用例覆盖

**A2-7 替换点分组推进**｜B
- 边界：替换面 ≥20 处跨 8+ 文件，一次全改回归难定位
- 建议：按文件/调用意图分组，每组替换后跑 ctest + 冒烟；组间独立提交

### A3 SessionManager 死代码清理（P1-6）——牵测试

**A3-1 legacy 测试引用清单（2026-09-03 取证完成）**｜A ✅
- 取证结果：
  - findIncomplete/removeAllIncomplete/promote：**零引用**（含测试——qa_CLFSessionManager.cpp:4 注释自承"已移除 3 个 _incomplete 旧语义用例"）→ 纯删，无测试障碍
  - SessionManager::save：测试引用 7 处（qa_CLFAgentLoop:493、qa_CLFSessionManager:83/117/130/133/135/315）→ **保留为测试设施**，注释标记"新代码不应使用"
  - CLFRepl::saveSession / CLFAgentLoop::saveSession：零引用生产死壳 → 删（AgentLoop::saveSession 删除后 SessionManager::save 的生产调用链完全断开，仅测试直接调用）
  - migrateLegacyIncomplete：main:162 生产调用 → 保留
- 建议（已并入分册 A3）：删 3 死函数 + 2 死壳；save 保留；删 AgentLoop::saveSession 时同步删 hpp 声明与注释

**A3-2 "留读不留写"边界**｜A
- 边界：分清哪些函数属 legacy 写入路径（删）vs 旧 .json 兼容读取（留，恢复旧会话用）；findIncomplete/promote 若仅服务 legacy 恢复流程则整体删
- 取证：旧会话恢复流程走哪条路径；removeAllIncomplete 的调用方
- 建议：按调用图裁，别按函数名猜

**A3-3 list 复用 loadJsonl 的容错语义**｜A
- 边界：list() 全量读文件取标题（388–420）与 loadJsonl（497–539）容错各异；复用后 /history 对损坏 jsonl 的表现会变——不能从"能列出"变"崩/空"
- 取证：两处对坏行/空文件/缺字段的处理差异
- 建议：容错语义统一以 loadJsonl 为准（或明确 /history 期望），坏行跳过 + 不崩，用例覆盖

**A3-4 别顺手改清理策略**｜A
- 边界：目录/清理策略（326–425）与存储耦合但不在 A3 范围
- 建议：A3 只删死代码；清理逻辑任何改动列入后续（若 P 项无归属则记录回流）

### A4 handler 脚手架 + 结果契约（P1-5 + F5）

**A4-1 返回字段契约先固化（改动前取证）**｜A
- 边界：handler 返回 ad-hoc JSON 字符串 → core formatToolResult 再解析；抽取 ok/err 封装若字段名/结构变 → core 解析崩。字段集是**跨层契约**
- 取证：列出现有 9 handler 返回 JSON 的字段全集 + formatToolResult 解析期望的字段
- 建议：先固化字段清单（success/content/error…）成文档/常量，ok/err 封装严格保字段；测试锁契约

**A4-2 todo_write 状态机别碰**｜A
- 边界：todo_write handler 内嵌 ~100 行状态机+持久化+UI 面板语义（195–290，195–290）——属"tools 越界操 core"问题（B/C 解决），不是脚手架
- 建议：A4 只收敛 parse/try/catch 骨架；状态机整体不动

**A4-3 各 handler 容错语义差异**｜A
- 边界：统一 parseJson 时各工具缺参处理不同（有的报错有的有默认）——统一包装不能抹平
- 取证：逐 handler 的缺参/坏参容错现状
- 建议：parseJson 返回 optional/错误对象，容错策略留在各 handler

**A4-4 契约结构化的改动面评估**｜C
- 边界：若 A4 引入结构化 ok/err（替代 JSON 字符串），formatToolResult + 测试 mock 同改——改动面比脚手架大
- 建议：拆两步——A4a 脚手架收敛（先行）；A4b 结果结构定型（可与 B1 后 executor 改造联动），pro 评估后定

---

## 三、批次 B 边界

### B1 CLFTool 能力标签（P0-2）

**B1-1 标签 vs SecurityPolicy 分工（2026-09-03 取证完成，行号修正）**｜A ✅
- 取证结果（executor 名字匹配点全集，**共 7 处**——原列 516/542 证伪，那两处是 useProgressive/isWriteTool 的 emit 判断非名字匹配）：
  - 369 `== "write_file" || == "edit_file"`（渐进发射判断）→ 可替代：`m_risk == Write`
  - 373-374 `find("search")/find("read")`（统计桶）→ 需新统计标签 m_isSearch/m_isRead
  - 400 `== "execute_command"`（危险确认入口）→ 可替代：`m_risk == Command`（**确认触发仍由 policy 按内容判定**，标签只定位"这是命令类工具"）
  - 416 `== "web_fetch"`（POST 升级入口）→ **保留运行时判断**（B1-2 证实参数驱动）；标签最多标"有远程副作用"用于确认流程
  - 434 `== "write_file" || == "edit_file"`（diff 预览触发）→ 可替代：`m_risk == Write`
  - 594-595 `find("read") || find("list")`（progressReads）→ 需统计标签 + 口径统一（B1-4）
- 建议（已并入分册）：m_risk 复用替代 2 类 + 新增 m_isSearch/m_isRead；一切按内容/参数的安全判定留 policy/运行时

**B1-2 web_fetch POST 是类型还是参数驱动？（2026-09-03 取证完成）**｜A ✅
- 取证结果：**参数驱动**——executor:416-429 读 `argJson.value("method", "GET")`，method=="POST" 才升级强制确认。不能静态打标一刀切
- 建议（已并入分册）：保留运行时判断；不引入 hasRemoteSideEffect 静态标签（web_fetch 默认 GET/HEAD 是读语义，风险级 Read 已正确）

**B1-3 execute_command 确认流程不动**｜A
- 边界：危险确认（400）由 policy 按内容判，标签只声明 runsCommand；确认弹窗逻辑不因标签化变化
- 建议：确认触发条件保持 policy 判定结果

**B1-4 统计口径（search/read 类）变化风险（2026-09-03 取证完成）**｜A ✅
- 取证结果：**现有两处口径已不一致**——readCount（373-374，`find("read")`，进 ToolStats）**不含** list_directory；progressReads（594-595，`find("read")||find("list")`，进 summary 文案）**含** list_directory。即 /context 统计与回合 summary 文案对 list_directory 的归类不同
- 取证：ToolStats.readCount/searchCount 被 getLastToolStats 消费（Repl submit 日志 + 可能 /context）；progressReads 仅 summary 文案
- 建议（并入分册）：先定口径再打标——**定案：read 集合 = {read_file, list_directory, web_fetch?}、search 集合 = {search_content}**（web_fetch 是否计 read 由 pro 执行时按现有文案期望定）；两处统一用同一标签，消除不一致

**B1-5 忘打标的静默不安全（bool 默认 false 隐患）**｜C
- 边界：标签默认 false → 新工具忘打标 = 无确认/无 diff 预览/无统计分类，**静默退化**
- 建议：不用裸 bool 默认 false——能力用枚举/位集且**注册时强校验**（关键工具类必须显式声明，缺失则注册报错）或默认"高安全"兜底

**B1-6 struct 扩展形式**｜C
- 边界：CLFTool 加字段影响 11 工具注册点 + 测试构造（qa_ToolExecutor mock）
- 建议：5 bool 还是位集 `Capability flags`（可扩展、可组合）；pro 定，倾向位集 + 强校验（呼应 B1-5）

**B1-7 渲染分支联动**｜A
- 边界：name.find("read") 可能还影响 formatToolResult 内容渲染分支（读文件结果展示）——匹配点清理要全（不止 executor 统计）
- 取证：grep 所有 name 字符串匹配点（executor 外也可能有）
- 建议：全仓 grep 工具名匹配，一处不漏

### B2 会话生命周期收敛 core（P0-4）

**B2-1 方法落点：AgentLoop 加方法 vs 先抽对象**｜C
- 边界：AgentLoop 已 god（P1-1）；往里加 startNewSessionFile/endSessionAndSummarize/resumeTo/appendTurnAndClose 4 方法加重——但 C2 才拆对象
- 建议：B2 先加方法（内部复用现有状态），C2 再搬实现进 CLFSessionFileCtx——pro 若评估直接抽对象更顺可提前，但需同步评估 C2 范围

**B2-2 UI/core 切分线（回显/确认留 UI，2026-09-03 R1 裁决更新）**｜A
- 边界：submit(911–988)、cmdClear(38–52)、/exit(28–36)、cmdResume(233–268) 各自混"策略动作 + UI 呈现 + 确认交互"；收敛边界 = core 收策略动作（含条件判定），UI 留呈现与确认
- 取证（已核）：core 原语序列 = cmdClear 5 原语（→ closeSessionAndReset 收编）、submit 轮初三步（**→ beginTurnSession 收编轮初两步条件判定**；appendTurnLine 无条件单步留 UI）；UI 专属 = 回显文案/setStatusKind
- 建议（定案）：两个新 core 方法 + UI 侧仅剩无条件原语序列 = P0-4 完整关闭

**B2-3 错误上报通道**｜C
- 边界：jsonl 创建/追加失败现在 UI 侧处理（显示什么？）；收敛进 core 后错误如何上报（ICLFOutput？返回值？）——通道不定义，UI 静默丢错
- 建议：core 方法返回结果对象/走 ICLFOutput 错误通道；与 B5（默认实现纪律）联动设计

**B2-4 跨线程状态纪律**｜A
- 边界：会话状态锁纪律逐字段演进（m_todos 有锁/m_activeSessionFile 有锁/m_resumedFrom 无锁/原子若干，hpp:202–212 注释自承"防御性/串行"）；B2 方法被 UI 线程调、状态被 agent 线程写——锁边界不清则数据竞争
- 取证：各状态成员的实际读写线程（agent 线程 vs UI 线程）
- 建议：收敛期统一锁纪律（新方法内部加锁或文档化线程契约），与 C2 的 CLFSessionFileCtx 统一锁衔接

**B2-5 /clear 六步状态机切分点（2026-09-03 取证完成，原断言证伪）**｜A ✅
- 取证结果：cmdClear(Commands:38-52) **无确认交互**（cmdExit/cmdResume 同样无确认）——六步 = 5 个 core 原语（closeSessionFileWithSummary → setResumedFrom → setTodos → setTodoPanelDone → clearContext）+ UI 呈现（setStatusKind(None) + emitContent 文案）
- 建议（并入分册 B2）：core 新增 `closeSessionAndReset()` 收编 5 原语序列；呈现留命令壳

**B2-6 resumeTo ↔ restoreSession 联动**｜A
- 边界：cmdResume(233–268) 调 resumeTo，恢复流程含 restoreSession UI 回显（P0-6/B4）；B2 先方法化、B4 再外移回显——顺序依赖，B2/B4 同批做时先 core 后 UI

### B3 UI 概念移出 core（P0-5）

**B3-1 m_todoPanelDone 真语义取证（2026-09-03 取证定案）**｜A ✅
- 取证结果（全调用点）：
  - 清（面板显示）：todo_write create/update（BuiltinTools:244/278）、resume 非全完成快照（AgentLoop:803）
  - 置（面板隐藏）：finishTurn 全完成收尾（AgentLoop:511）、submit 新回合（Repl:952）、/clear（Commands:46）、resume 全完成快照（AgentLoop:803）
  - UI 只读消费：Repl 渲染（462-463）
  - 与 m_turnStartMsgCount **无联动**（原假设证伪）——那是 appendTurnLine 差集基准；与 m_todoDirty 独立（dirty 管落盘快照，panelDone 管展示）
- **语义定案：回合级 todo 面板展示生命周期状态，core 在回合边界维护，UI 只读**。无用户交互置位（非"用户收起面板"）
- 方案（并入分册 B3）：UI 自推导不可行（全完成收尾判定在 core finishTurn 内部）→ **接口保留 + hpp 注释修正为真语义，C2 时随 CLFTodoStore 迁移**；不改名（改名动 6 处调用点零收益）；跨线程已安全（atomic store/load）

**B3-2 替代通道与 ICLFOutput 联动**｜C
- 边界：若选事件/回调 → 通道加在哪（ICLFOutput 新默认方法 onTodoChanged? 与 B5 默认实现纪律一致）；若选 UI 自推导 → UI 如何感知 todos 变化（m_todoDirty 原子标志？）
- 建议：优先 UI 自推导（改动最小，若语义允许）；事件通道次选

**B3-3 测试影响**｜A
- 边界：qa_AgentLoop 若断言 todoPanelDone 状态 → 移除后测试改
- 取证：测试引用点清单

### B4 恢复回显外移（P0-6 + P1-7）

**B4-1 结构化行 = jsonl 类型协议集中契机（补 P1-7）**｜A
- 边界：restoreSession(815–890) 解析 jsonl 生成 📋/✓/⏳ 文案；外移需返回结构化行（type+数据）——行类型字符串现在分散 Codec/SessionManager/AgentLoop/测试（P1-7），B4 顺带集中类型常量 + 解析注册
- 取证：现有 jsonl 行类型全集 + 各处字符串字面量
- 建议：类型常量单点 + 注册表；**未知类型静默跳过（唯一安全网）保留**

**B4-2 回显视觉保真**｜B
- 边界：UI 层拿结构化行重建折叠块与文案（📋/✓/⏳ + showFoldedBlock）——恢复会话的视觉输出要与 v0.6.0 一致
- 建议：验收对比旧版恢复显示（截图/录屏 or 逐字符 diff 文本流）

**B4-3 旧 jsonl 兼容**｜A
- 边界：旧会话文件（不同时期格式/缺字段）解析容错不变
- 建议：用真实旧会话样例（用户 v0.5/0.6 会话文件）做恢复回归

### B5 ICLFOutput 纪律（P1-2）

**B5-1 注释修正**：头注释"6 个方法"→ 实际（12 纯虚+3 默认+2 非虚）；规则写入注释
**B5-2 别顺手拆接口**：拆窄接口是 C3，B5 只做纪律（新方法带默认实现 + 注释），防范围蔓延

---

## 四、批次 C 边界

### C1 FileOps/CLFDiff 归属修正（P1-12，方向已修订：独立目标雏形）

**C1-1 include/依赖图取证（一次画对）**｜A
- 边界：独立 clf_capabilities 目标需理清 FileOps/CLFDiff 的 include 清单与调用方（CLFToolExecutor、CLFBuiltinTools handler?）——现在 hack 正因 core↔tools 循环，理不清则新目标又造环
- 取证：FileOps/CLFDiff 的 #include 全集 + 全部调用点归属 target
- 建议：先画 target 依赖图（clf_capabilities ← clf_core/clf_tools 单向），再落 CMake

**C1-2 clf_tools 是否也链 clf_capabilities**｜A
- 边界：CLFBuiltinTools（clf_tools）若调 FileOps → clf_tools 也依赖 clf_capabilities；clf_ui 呢？——依赖面全列出
- 取证：各 target 对 FileOps/Diff 的引用
- 建议：依赖单向向下（capabilities 不依赖 core/tools/ui），谁用谁链

**C1-3 出口标准一致性**｜C
- 边界：阶段 1 出口含"无 CMake hack、依赖图干净"——若 C1 推迟到阶段 2（试点时以插件边界一步到位）则阶段 1 出口不达标；若本批做独立目标则达标
- 建议：**阶段 1 内做最小独立目标**（消除 hack + 分层落地），不推迟——与阶段 2 试点（FileOps 迁 DLL）衔接而非重复

**C1-4 header vs cpp 归属**｜A：FileOps/Diff 哪些实现可 header-only、哪些留 cpp；目标内文件组织一次定好（防再次错位）

### C2 AgentLoop 拆角色（P1-1）

**C2-1 7 状态成员分布 + 锁随对象走**｜A
- 边界：7 会话状态成员（m_todos/m_todoPanelDone/m_todoDirty/m_activeSessionFile/m_resumedFrom/m_historyDir/m_turnStartMsgCount）分布到 CLFSessionFileCtx/CLFTodoStore——锁纪律各异（4.5/B2-4），拆后**锁随对象**统一
- 建议：先 B2（方法化+纪律）后 C2（搬实现），避免边拆边乱

**C2-2 restoreSession 落点**｜A：B4 后 core 不再排版但解析仍属 core——CLFSessionFileCtx 或 SessionManager 持有解析，UI 消费结构化行
**C2-3 依赖具体类 → 接口化**｜A
- 边界：ProtocolAdapter/SecurityPolicy（值成员）、Summarizer（unique_ptr 具体类，hpp:195–200）非接口——C2 是否一并接口化（HttpClient 已有 ICLFHttpClient 先例）
- 取证：三者的构造注入点与测试 mock 方式（qa_AgentLoop 怎么构造 AgentLoop）
- 建议：评估面大则拆 C2a（拆角色，纯搬移）/C2b（接口化，动构造+mock）；pro 评估
**C2-4 测试构造变化**｜A：拆对象后 qa_AgentLoop 构造/注入方式变——测试同步清单
**C2-5 与 B3 顺序**：AgentLoop 去 UI 概念（B3）须先于/同批 C2，否则 C2 拆出的对象又带 UI 概念

### C3 ICLFOutput 拆窄接口（P1-2 深化）

**C3-1 方法归位表（逐方法裁决）**｜A
- 边界：17 方法归四接口（内容/状态/交互/辅助）有跨通道方法（showProgress=状态 or 内容？appendThinking=辅助?）——归位错误造成调用方依赖错
- 建议：先出 17 方法 → 四接口归位表，pro 逐行裁决后再动
**C3-2 实现聚合形式**｜C：CLFTerminal 多继承 vs 组合持有——多继承同名方法冲突风险；pro 定
**C3-3 调用面改动**｜A：core 依赖窄接口（AgentLoop/Executor/Repl 构造参数变化 + 4 mock 最小实现集）——改动面清单先行，C 批最后做（成本最高）
**C3-4 mock 分化**｜A：qa_AgentLoop/qa_ToolExecutor/qa_Passerby/qa_TipsBar 各自实现 ICLFOutput 哪些方法 → 拆后各 mock 最小集

### C4 Terminal 封装（P1-3/P1-4）

**C4-1 public 直写点清单（一处不漏）**｜A
- 边界：Terminal 状态几乎全 public（hpp:94–126），Repl 直写 m_contentBuffer/m_confirm*/m_interruptCb（Repl:121/554–576/768/791）——封装成窄操作需**逐直写点映射**，漏一个 = 编译错 or 行为变
- 取证：grep 所有对 Terminal public 成员的直写点 → 映射 appendLine/submitConfirm/cancelConfirm/setInterruptCb 等
- 建议：先清单后封装；A1 已拆 Repl → 直写点集中在 View/InputHandler 两处，封装更顺（顺序依赖 A1）
**C4-2 确认协议收敛时序**｜A
- 边界：确认框跨 Terminal::confirm + Repl 事件 + ConfirmBar 手编 CV/flags——收敛后确认交互时序（用户按键 → 回调 → 结果回 agent 线程）不变；跨线程唤醒单点化
- 取证：确认全链路（发起方/等待方/唤醒/取消/超时）现状
- 建议：协议收敛 Terminal 单类；确认期并发输入/取消行为用例覆盖
**C4-3 测试路径**｜A：现有确认/交互靠人工冒烟（无 Terminal 自动化？）——C4 后若需自动化评估成本；冒烟清单加确认专项
**C4-4 别引入接口化**：C4 是封装（状态 private + 窄操作），不是把 Terminal 抽象成接口（无此 P 项）——防范围蔓延

---

## 五、回归与兼容边界（跨批次）

| # | 边界 | 取证/验证 |
|---|---|---|
| T1 | **ctest 套件构成** | 列出现有 qa_*（AgentLoop/ToolExecutor/Passerby/TipsBar…）与数量；每批全量跑 |
| T2 | 真实旧会话兼容 | 用用户 v0.5/v0.6 真实 jsonl 会话文件做恢复回归（损坏行/空文件/BOM/旧格式缺字段） |
| T3 | 多字节/emoji 输入路径 | A2 边界用例：中文/emoji/4 字节字符/组合字符的截断、宽度、选区、回显 |
| T4 | 安全行为回归（B1 后） | 危险命令确认、工作区边界、web_fetch POST——行为与 v0.6.0 一致（加"新工具"假想演练：只打标不改进程，验证 executor 免改） |
| T5 | 交互保真（A1/B2/B4/C4） | 对话/tool 调用/确认/粘贴/折叠/Ctrl+T/Ctrl+R/恢复会话冒烟五条路径每批必跑 |
| T6 | 输出零漂移（纯搬移批） | A1/A3 等"纯搬移"可用文本流 diff（非交互模式 + 固定输入）对比 v0.6.0 输出 |
| T7 | 锁/线程回归 | B2/C2 后跑压力/长会话冒烟（jsonl 追加与 todo 更新并发路径），无崩溃/无数据竞争告警 |
| T8 | 构建目标回归 | 每批构建产物一致（无新 hack、无重复符号、/MD 一致）——C1 后重点 |

---

## 六、销号表（2026-09-03 pro 评审取证，逐条已核）

| 编号 | 结论 | 证据摘要 |
|---|---|---|
| A1-1 | ✅ 证实（补充清单） | 捕获成员 ~15（m_showThinking/m_selection/m_lastRowMap/Texts/Styles/m_lastSnapshot/m_foldJustToggled/m_needRestoreInput/m_lastSubmittedInput/m_escCleanupFrames/m_lastEscTime/m_inputHistory/m_historyIndex/m_historyDraft/m_tipsBar 等）+ 局部 ~10（inputText/cursorPos/scrollView/asyncSubmit/pasteCoalescer/confirmBar/screen/dbgEvt…）。建议不变：先列清单，共享量收敛为显式状态对象 |
| A1-2 | ✅ 证实（分发表已整理） | 事件分支与消费语义已逐分支记录（0a doSubmit/0b 粘贴窗满/1 确认激活最小化/1.4 选区接管/1.45 选区进入/1.5 粘贴路由/2 Ctrl+D/3 Ctrl+N/Ctrl+T/Ctrl+R/4a 历史/4 Ctrl+C/5 Esc/6 Tab/7 Shift+Tab/8 滚动/通用 CPR 剥离）。拆分时返回值语义逐一对照 |
| A1-3 | ✅ 证实（两折叠状态本就独立） | 思考折叠 = Repl m_showThinking（706 写 / 294 读）；恢复折叠 = Terminal m_foldedExpanded（showFoldedBlock/toggleFoldedBlock）。各随归属走，无分裂风险 |
| A1-4 | ✅ 证实 | m_selection + m_lastRowMap/Texts 均为 Repl 成员，Renderer（338 读）与 CatchEvent（604-652 写）共享同一实例。随共享状态对象传递 |
| A1-5 | ✅ 证实 | 临时文件清理(108-114)、FTXUI 初始化(116-125)、Win32 控制台修复(858-872)、join 收尾(873-876) 属壳层 |
| A1-6 | ✅ 证实 | printBanner(893-909) 内 CLFSkillLoader::loadFromDir（906）副作用确认。留壳层，P2-1 不顺手修 |
| A1-7 | ✅ 证实（建议定：留壳层） | submit(911-988) 依赖 m_dispatcher/m_agent/m_output/m_passerby/m_lastTsDate。**定案：留 CLFRepl 壳层**（B2 收编），A1 只拆渲染与事件主体 |
| A1-8 | ✅ 证实（同构可抽） | 两段剥离（183-203 / 825-845）逐字同构，处理对象同为 inputText。抽 stripCprResidual(std::string&) |
| A2-1 | ✅ 修订（见条目，单安全版） | 逐点取证：无精确字节场景；统一 utf8SafeHead |
| A2-2 | ✅ 证实（实现等价） | SelectionModel::charWidth（cpp:13-17）与 Terminal cjkWidth（cpp:198-207）逻辑等价（首字节≥0xC0 计 2）。合并安全；共同局限 emoji 计 2（一致，不产生错位） |
| A2-3 | ✅ 证实（语义有差异） | AgentLoop appendSplitLines（30-42：空文本 emplace_back 空行、不处理 \r）；Terminal 两处（91-100/384-392：\n 切分、尾空行丢弃）。pro 执行逐点核对 |
| A2-4 | ✅ 修订（见条目） | 6 处全已 localtime_s/_r；收敛动机 = 消 ifdef 重复 |
| A2-5 | ✅ 证实（定案：clf_types） | 调用面跨 core（SessionManager/Summarizer）+ tools（WebFetch）+ ui（Terminal/Repl）；clf_types 对所有层可见。新工具入 CLFEncoding 同目标（clf_types），不新建 target |
| A2-6 | ✅ 证实 | isValidUtf8 在 CLFEncoding（CLFTypes/CLFEncoding.hpp）。utf8SafeHead 对非法输入行为：先定"原样返回不崩" |
| A2-7 | ✅ 证实 | 替换面 ~20 处跨 8+ 文件（7 安全截断 + 6 ToolExecutor + 2 Summarizer + Repl wrap + 6 时间戳 + 3 换行 + 2 ANSI）。按文件分组替换 |
| A3-1 | ✅ 修订（见条目，清单已出） | 3 死函数零引用；save 测试引用 7 处保留；2 个 saveSession 死壳删除 |
| A3-2 | ✅ 修订 | 留读不留写成立：load/parseFull 被 restoreSession/list 调用（读留）；save 写入生产零调用（测试设施保留）。findIncomplete/promote 仅服务 legacy 流程 → 整体删 |
| A3-3 | ✅ 证实（容错差异小） | list jsonl 分支（393-400）parse 异常 → stem fallback 不崩；loadJsonl 坏行跳过。复用后 /history 表现不劣化；用例覆盖损坏首行 |
| A3-4 | ✅ 证实 | cleanupOld(674-700)/list 目录清理策略独立，A3 不动 |
| A4-1 | ✅ 证实（契约已固化清单） | handler 返回字段全集：success/error/content/todos/path/written/status/headers/body/truncated/binary/exitCode/stdout/stderr/timedOut；formatToolResult 解析：exitCode→stderr→stdout / success→error / content→stdout。ok/err 封装严格保字段 |
| A4-2 | ✅ 证实 | todo_write 状态机（195-290）100 行内嵌 setTodos/markTodosDirty/appendTodoSnapshotNow/setTodoPanelDone——B/C 解决，A4 不碰 |
| A4-3 | ✅ 证实（容错各异） | 逐 handler 缺参策略不同（webFetch url 必填报错/read_file path 空→边界报错/todo 默认 list）。parseJson 返回 optional，容错留各 handler |
| A4-4 | ✅ 证实（拆两步） | A4a 脚手架先行；A4b 结果结构化与 B1 后 executor 改造联动 |
| B1-1 | ✅ 修订（见条目，7 处匹配点） | 369/373-374/400/416/434/594-595；516/542 原列证伪 |
| B1-2 | ✅ 修订（参数驱动） | executor:416-429 method 参数驱动；保留运行时判断 |
| B1-3 | ✅ 证实 | 危险确认（400）policy 按内容判，确认弹窗逻辑不动 |
| B1-4 | ✅ 修订（口径不一致发现） | readCount 不含 list_directory、progressReads 含——先定口径（read={read_file,list_directory}、search={search_content}，web_fetch 由 pro 定）再打标 |
| B1-5 | ✅ 修订（简化方案） | 不引入裸 bool 标签——m_risk 复用 + 仅新增统计标签；忘打统计标 → 仅统计缺失低危。原"注册报错强校验"必要性消失 |
| B1-6 | ✅ 证实（定：2 个 bool） | CLFTool 现有 6 字段（含 m_concludesTurn 先例）；注册点逐个字段赋值非聚合初始化 → 加带默认值字段不破坏编译。定案：m_isSearch/m_isRead 两个 bool（位集 2 位不值） |
| B1-7 | ✅ 证实（匹配点全集已列） | 全仓工具名匹配 = executor 7 处 + BuiltinTools 注册名定义（非匹配）；formatToolResult 无名字匹配 |
| B2-1 | ✅ 修订（定：双新方法，R1 裁决后） | 原 4 方法对应原语大多已存在；新增 closeSessionAndReset() 收编 cmdClear 5 原语 + **beginTurnSession() 收编 submit 轮初两步条件判定**；appendTurnLine 无条件单步留 UI（收进 runTurn 会改变非交互模式行为面） |
| B2-2 | ✅ 证实（切分线清单） | cmdClear 5 core 原语 + UI 呈现（emitContent 文案/setStatusKind）；cmdExit 1 原语+文案；cmdResume restoreSession+文案；submit beginSessionFile/appendTurnLine/setTodoPanelDone |
| B2-3 | ✅ 修订（无需新通道） | 现有原语全部 void + 内部 warn 不抛；UI 无错误处理职责。维持现状 |
| B2-4 | ✅ 证实 | m_resumedFrom 无锁（工作线程串行，hpp 注释自承）；m_activeSessionFile 有 m_sessionCtxMutex；m_todos 有 m_todosMutex；atomic×2。closeSessionAndReset 内部遵循既有纪律 |
| B2-5 | ✅ 修订（证伪，见条目） | 无确认交互；5 原语收编 |
| B2-6 | ✅ 证实 | cmdResume(233-268) 调 restoreSession（含 UI 回显 P0-6）；B2 先方法化、B4 再外移回显 |
| B3-1 | ✅ 定案（见条目） | 回合级展示生命周期；接口保留 + 注释修正 + C2 随 CLFTodoStore 迁移 |
| B3-2 | ✅ 修订（定：接口保留） | UI 自推导不可行；事件通道零收益。ICLFOutput 不加 onTodoChanged |
| B3-3 | ✅ 证实（零测试影响） | qa_CLFAgentLoop 无 todoPanelDone 状态断言（grep 零命中） |
| B4-1 | ✅ 证实（字面量清单已出） | 类型字面量 3 文件 ≥12 处（MessageCodec.cpp:257-369、SessionManager:496-539、AgentLoop:834/862）；未知类型静默跳过已在 loadJsonl:540 |
| B4-2 | ✅ 证实（视觉保真素材） | 回显文案三处：本轮清单（852-858）、complete 行（865-868）、折叠块 summary（884-886）。UI 重建对照 v0.6.0 输出 |
| B4-3 | ✅ 证实（样例存在） | doc/contextHistory/ 8 个 jsonl（含续写）+ latest.json 旧格式样例 |
| B5-1 | ✅ 证实 | hpp:23 "6 个方法, 5 组"过时；实际 12 纯虚 + 3 默认 + 2 非虚 |
| B5-2 | ✅ 证实 | 拆窄接口是 C3，B5 不做 |
| C1-1 | ✅ 证实（依赖图已画） | FileOps include：CLFEncoding（clf_types）+ 系统头；Diff include：零项目依赖。调用方：ToolExecutor（core）、BuiltinTools（tools）。依赖单向：clf_capabilities ← core/tools |
| C1-2 | ✅ 证实 | clf_tools（BuiltinTools）链 clf_capabilities；clf_ui 不直接引用 FileOps/Diff（grep 零命中）。谁用谁链 |
| C1-3 | ✅ 证实 | 阶段 1 内做独立目标（消除 hack）；与阶段 2 FileOps 迁 DLL 衔接 |
| C1-4 | ✅ 证实 | FileOps 有 windows.h 分支留 cpp；Diff 纯算法可 header-only 但保持 cpp 与现状一致 |
| C2-1 | ✅ 证实（分布定案） | 7 状态成员分布：CLFTodoStore（m_todos/m_todosMutex/m_todoPanelDone/m_todoDirty）、CLFSessionFileCtx（m_activeSessionFile/m_sessionCtxMutex/m_resumedFrom/m_historyDir/m_turnStartMsgCount）；锁随对象 |
| C2-2 | ✅ 证实 | B4 后 restoreSession 解析归 CLFSessionFileCtx/SessionManager，UI 消费结构化行 |
| C2-3 | ✅ 证实（拆 C2a/C2b） | http 已接口化（qa mock 注入 ✓）；ProtocolAdapter/SecurityPolicy 值成员、Summarizer unique_ptr 具体类非接口。C2a 拆角色先行（纯搬移），C2b 接口化后置评估 |
| C2-4 | ✅ 证实 | qa_CLFAgentLoop 经 makeAgent(mock) helper 构造（97 行）；拆对象后构造注入变化清单同步 |
| C2-5 | ✅ 证实 | B3（去 UI 概念）先于/同批 C2——定案：同批（B3 轻量，仅注释+归属） |
| C3-1 | ✅ 证实（归位表已拟，R3 修正） | 17 方法归位：内容（emitContent/emitRaw/emitStyledLine/appendThinking/clearThinking/**showFoldedBlock**——折叠块是展示输出非交互）、状态（setStatus/setStatusTextOnly/setStatusKind/requestRefresh/showProgress/finishProgress）、交互（confirm/onInterrupt）、辅助（notifyActivity/activityCount）。pro 执行逐行裁决 |
| C3-2 | ✅ 证实 | CLFTerminal 组合持有四接口（多继承同名方法冲突风险规避） |
| C3-3 | ✅ 证实 | 调用面：AgentLoop/Executor（core）、Repl（ui）、4 mock（qa）。C 批最后做 |
| C3-4 | ✅ 证实（4 mock 清单） | qa_CLFAgentLoop:122、qa_CLFPasserby:17、qa_CLFToolExecutor:24、qa_CLFTipsBar:21 各自 MockOutput；拆后各 mock 最小集 |
| C4-1 | ✅ 证实（直写点清单已出） | Repl 直写 8 处（121/122 m_contentBuffer/m_pendingLine、179 m_refreshPending、559-575 m_interruptCb/m_confirmMutex/m_confirmResult/m_confirmCv/m_confirmActive/m_confirmSel、588 m_confirmSel）+ ConfirmBar 读 4 处（22/23/31/41） |
| C4-2 | ✅ 证实（全链路已画） | 发起 Terminal::confirm（CV wait）→ 唤醒 Repl CatchEvent 置位+notify → 取消 cancelWithInterrupt（先中断后唤醒）→ 渲染 ConfirmBar。收敛 Terminal 单类 |
| C4-3 | ✅ 证实 | 确认/交互现靠人工冒烟；C4 后冒烟清单加确认专项 |
| C4-4 | ✅ 证实 | C4 封装非接口化，防范围蔓延 |
| T1 | ✅ 证实 | ctest 套件 21 个 qa target（CMakeLists CLF_TEST_TARGETS） |
| T2 | ✅ 证实 | 真实旧会话样例齐备（8 jsonl + latest.json） |
| T3 | ✅ 证实（R4 补强） | A2 边界用例含中文/emoji/4 字节/组合字符；**加 CJK 长输入换行视觉回归**——Repl:270 pendingLine 字节 wrap → substrByWidth 是渲染行为变更（换行点会变），须人工对照 v0.6.0 |
| T4 | ✅ 证实 | 安全行为回归 + 新工具假想演练（只打标不改进程） |
| T5 | ✅ 证实 | 冒烟五路径每批必跑 |
| T6 | ✅ 证实 | 非交互模式文本流 diff 对比 v0.6.0 |
| T7 | ✅ 证实 | 长会话冒烟（jsonl 追加 + todo 并发） |
| T8 | ✅ 证实 | 构建目标回归（C1 后重点：无新 hack、无重复符号、/MD 一致） |
