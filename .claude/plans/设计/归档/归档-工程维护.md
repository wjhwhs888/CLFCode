# 归档-工程维护

> 状态：✅ 已完成 | 提交：1d17cfe / c55e023

## 1. main.cpp 模块化拆分（1d17cfe）

**问题**：main.cpp 膨胀到 230 行，混合工具 handler、注册、配置解析、REPL 多种职责。

**方案**：
- 工具 handler + 注册 → `CLFTools/CLFBuiltinTools.hpp/.cpp`（`registerBuiltinTools()`）
- 配置路径解析 → `CLFConfigLoader`
- main.cpp 精简到 ~80 行（加载配置 + 注册工具 + REPL）

**原则**（写入 CLAUDE.md）：main.cpp 只做入口编排，具体实现归属对应功能模块。

## 2. 项目根目录统一管理（1d17cfe）

**问题**：`resolveConfigPath()` 三层向上试探（./ ../../ ../），config/skills/constitution 各写一遍。

**方案**：`findProjectRoot()` 从 exe 位置向上找 `CMakeLists.txt`，`resolvePath()` 基于项目根统一解析。

## 3. 全量体检修复 4 个 bug（c55e023）

| Bug | 严重度 | 问题 | 修复 |
|-----|:---:|------|------|
| Windows 命令输出为空 | 🔴 | CLFCommandExec Windows 分支读取是 TODO | 实现读取 + 临时文件唯一化（进程ID）+ 清理（**注：后续重构改用 CreateProcess + 匿名管道，不再使用临时文件**） |
| SSE 跨 chunk 丢数据 | 🔴 | 半行 JSON 直接被丢弃 | CLFHttpClient 行缓冲 + 流末冲刷 |
| 超时配置未生效 | 🟡 | max_response_delay_sec 未接通 HTTP 超时 | 构造时 setTimeout |
| 流式 tool_calls 丢失 | 🟡 | 仅 [DONE] 时 finalize | 收到 finish_reason 即 finalize |

## 4. 验证

- `> 运行 dir` → stdout 完整读取、无临时文件残留 ✅
- 长回复流式输出不丢字 ✅
